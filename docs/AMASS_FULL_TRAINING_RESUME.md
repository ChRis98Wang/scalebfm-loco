# 全量训练续跑记录

## 最新进展：2026-09-06 已继续

50 次全库数值短训已于 12:23:22 完成并通过检查，后续 950 次全量微调已启动。
当前运行名、指标口径和停止方式以 [全量训练运行记录](AMASS_FULL_TRAINING_RUN.md)
为准。下文保留启动前的暂停交接快照，不代表现在仍未开训。

CNRS 异常已使用本地实际的 `joblib.load` 正确读取并追到原始轨迹跳变，本轮继续
排除该动作，没有修改或重建数据；详情见最新运行记录。

## 暂停状态：2026-09-06

用户要求“明天继续”，因此已停止本次启动流程。**尚未启动全量训练，没有创建全量
训练服务**；本次只完成数据检查与训练/验证索引准备。不要把已经发布索引视为已开训。

沿用现有 IsaacLab，不重装环境。原始归档、原始动作、重定向结果及已有 checkpoint
均未删除或覆盖。此前 2026-09-05 的小规模 100 + 900 次微调已完成，但没有证明优于
官方起点；下一轮默认仍从官方 M 权重开始。

## 已完成

对 5,253 条去重素材重新验证了打包文件 SHA-256、格式、维度、有限值和四元数范数，
全部通过结构检查。另作数值异常筛选，并固定目录标识层面的受试者划分。

| 集合 | 动作数 | 时长（秒，50 Hz） |
| --- | ---: | ---: |
| 训练 | 4,247 | 36,053.64 |
| 验证 | 962 | 7,810.26 |
| 本轮排除 | 44 | 127.88 |

- [训练索引](../ScaleRetarget/retargeted_dataset/amass_full_v1_train.yaml)
- [验证索引](../ScaleRetarget/retargeted_dataset/amass_full_v1_validation.yaml)
- [排除索引](../ScaleRetarget/retargeted_dataset/amass_full_v1_excluded.yaml)
- [完整筛选规则与统计](../logs/amass_preparation/full_v1_manifest.json)

原小规模训练与验证动作分别保留在各自新集合中，整个受试者分组没有交叉；新划分
还检查了源文件 SHA-256 无交叉。跨数据集真实人员身份和官方预训练是否见过这些动作
未知，不能宣称完全独立于预训练数据的最终测试集。

排除 44 条仅通过新索引实现，没有删除数据：20 条 BMLrub 动作不足 1 秒；23 条 ACCAD
`MartialArtsWalksTurns_c3d` 的受试者身份无法可靠确认；1 条 CNRS 动作出现极端根轨迹。
其余动作不按“只保留站立行走”筛选，跑跳、蹲起、上肢等类别仍在本轮数据池中。

## 明天继续的入口

1. 先确认没有重复训练任务、显存与内存有余量；重新检查上述三个索引和来源哈希。
2. 复核 `CNRS/283/0_R_2_stageii` 的异常来源：打包数据在 50 Hz 下测得骨盆最大相邻
   帧速度约 82.36 m/s，已从本轮排除。暂停前追踪源文件与重定向结果时，使用标准
   `pickle.load` 读取该 `.pkl` 失败（`invalid load key, 0x04`）；这是诊断工具未按
   实际序列化方式读取，不能据此认定重定向文件损坏。应先查看现有 loader 使用的
   读取方法，不修改或重建数据。尚未完成跨阶段根因归属。
3. 使用现有 `/home/sw/isaaclab_ws/env_isaaclab_sim6_newton/bin/python`，任务
   `G1-BFM-Transformer-Tracking`，官方起点
   `humanoid_transformer_m/model_22200.pt`，128 个环境、无 GUI、种子 42。
4. 先做全量加载和有明确迭代上限的短训检查，再决定后续长训。具体迭代数尚未启动
   或锁定，需在启动时记录。全库评估比小样慢，应明确其频率和单条评估时长上限，
   不沿用小样的一小时估算，也不要把跳过评估的加载探针称为效果验收。
5. 必须使用唯一运行名和独立、限时的受控进程组。可沿用 systemd 用户服务的
   `KillMode=control-group`、`KillSignal=SIGINT`、`TimeoutStopSec=20s`、
   `SendSIGKILL=yes`、`Restart=no` 和 `RuntimeMaxSec`；启动后核实真实训练迭代，
   结束或中断后核实本轮全部子进程退出。

未修改 PPO 网络或算法，尚未新增数据集均衡采样逻辑。BMLrub 动作较多、时长占比较高，
后续应分别观察各来源与动作类别表现，不只看混合平均指标。
