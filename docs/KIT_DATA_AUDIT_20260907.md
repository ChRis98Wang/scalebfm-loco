# KIT 新数据压缩包检查

日期：2026-09-07。用户提供 `/home/sw/Downloads/KIT.tar.bz2`。

后续更新：4231 条有效输入已全部重定向/打包并完成全量审计，1 条单帧源文件保留
但排除。新增 2927 条训练动作，扩展训练池至 7174 条，并已完成实际 KIT 单步训练
接入验收。详见 [KIT 入库记录](KIT_INGESTION_20260907.md)。
下文保留最初只检查归档时的快照，不代表当前状态。

结论：归档可以完整遍历，抽样数据符合当前 AMASS SMPL-X Stage-II 输入形式，
可进入后续逐条校验与重定向准备。**本次未解压入库、未重定向、未修改训练/验证
索引、未启动训练。** 4232 是候选动作文件数，不是已经验证可训练的动作数。

## 文件与空间

- 压缩包大小：3,636,030,360 字节，约 3.39 GiB。
- 文件最后修改时间：2026-09-07 12:52:36 CST；检查前后大小、修改时间及 inode 未变化。
- `tar -tvjf` 完整遍历返回 0，无解压错误。检查结束后没有发现持有该文件的进程。
- 归档 4344 项，其中普通文件 4288 个、NPZ 4287 个，`*_stageii.npz` 4232 个。
- 普通文件尺寸合计：10,608,829,500 字节，约 9.88 GiB；尚未落盘解压。
- 原压缩包与解压内容合计约 13.27 GiB，不包含未来重定向、50 Hz 打包和日志。
- 检查时所在文件系统剩余约 551 GiB，足以存放本次归档及解压内容。

归档内容指纹（用于后续对应同一文件；不是与发布方校验和比对）：

```text
SHA-256: 0f27388ec6ff3af31a54ef424f4d5d567d214d8389403f0914bf52021f962fa0
```

本次归档列表未观察到符号/硬链接；后续解压仍需独立验证全部成员路径、类型和目标
目录边界，不能把命令行列表检查当成安全解压器。

## 抽样内容

直接从归档内存读取 `KIT/63/Chris3MSeminar_Cal_08_stageii.npz`，没有写出原始数据，
使用 `numpy.load(..., allow_pickle=False)`：

| 字段 | 样本内容 |
| --- | --- |
| surface_model_type | smplx |
| gender | neutral |
| mocap_frame_rate | 100 Hz |
| poses | (259, 165)，数值有限 |
| trans | (259, 3)，数值有限 |
| pose_body | (259, 63)，数值有限 |
| root_orient | (259, 3)，数值有限 |
| betas | (16,)，数值有限 |

归档列表中可见 turn_left、upstairs/downstairs、slope、balance_on_beam 等动作名称。
这里只核对了一条 Stage-II 的核心数组；不能据此认定全部片段均有效、均为同一帧率，
或机器人必定能完成名称对应行为。外层归档可读也不替代每个 NPZ 的全量数据验证。

## 与现有工作衔接

当前 `amass_full_v1_train.yaml` 与 `amass_full_v1_validation.yaml` 未发现 KIT 条目，
原始目录 `ScaleRetarget/dataset/amass/KIT` 尚不存在，因此它是尚未纳入本地流水线的
新来源；尚未做与现有片段的内容哈希/近重复检查。

后续顺序：安全解压到独立 KIT 目录 → 逐条 Stage-II 校验 → 去重及剔除不合适的
标定/短片段 → G1 29 DoF 重定向 → 50 Hz 打包和来源审计 → 创建新版本数据索引。
保留原训练/验证索引及 962 条验证基线，不把新增数据直接混入旧评估口径。

新动作数据有助于扩展运动覆盖，但不等于物体接触监督、材料实测数据或任务控制器。
BFM 功能现状和开发优先级见 [功能对照表](BFM_CAPABILITY_MATRIX.md)：
先完善核心控制；箱子交互使用已确认的双臂夹持方案，不新增手指。

本次所有归档检查和八模式仿真检查均已结束，无本次任务的常驻进程。
